# DESIGN.md · 媒体运营平台（Apple Light Warm）

本文档由 `integrations/awesome-design-md/design-md/apple/DESIGN.md` 蒸馏，是本工作台唯一 UI 设计基线。任何 AI 改动 UI 前必须先读本文件，新页面/组件默认遵循本节规范，不引入暗黑霓虹或深色科技风。

## 1. 设计语言

- 风格：浅色、暖色、Apple HIG 风格；信息优先、克制装饰、层次清晰。
- 背景：暖白/米白（`#faf5ef` → `#fdf9f3`），大卡片近纯白。
- 主强调色：暖橙（`#ea580c` / `#f59e0b`），用于主行动按钮、选中态、关键数据。
- 辅色：Apple 蓝 `#0071e3`（链接）、紫 `#af52de`、粉红 `#ff375f`、绿 `#34c759`、天空蓝 `#5ac8fa`，仅用于图谱模块区分。
- 字体栈：`-apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display", "PingFang SC", "Microsoft YaHei", system-ui`；代码 `SF Mono / Cascadia Code`。
- 圆角：按钮 10-12px、卡片 14-20px、对话框 20px；不使用夸张尖角。
- 阴影：暖灰阴影 `0 2px 12px rgba(120,90,60,.06)`，弹层 `0 18px 60px rgba(120,90,60,.16)`。
- 毛玻璃：顶栏、导航、Dock、悬浮面板使用 `rgba(255,255,255,.72~.9) + backdrop-filter: saturate(180%) blur(22px)`。

## 2. 颜色 Token

| Token | 值 | 用途 |
| --- | --- | --- |
| `--bg` | `#faf5ef` | 页面底色 |
| `--panel` | `rgba(255,255,255,.86)` | 卡片/面板 |
| `--text` | `#1d1d1f` | 主文字 |
| `--muted` | `#6e6e73` | 次要文字 |
| `--cyan` | `#d96c1f` | 通用暖强调 |
| `--amber` | `#e8890c` | 状态/预警 |
| `--green` | `#2f9e44` | 成功/已勾选 |
| `--red` | `#e0352b` | 错误/删除 |
| `--border` | `rgba(139,118,96,.16)` | 分隔与描边 |

## 3. 图谱首页

- 画布：暖白毛玻璃，柔和的橙色径向光晕；保留节点连线虚线的动态流动，但连线使用暖灰 `rgba(191,154,119,.45)`。
- 节点：外圈白底 + 彩色描边，内核填充模块色；文字 `#2b2117` / `#6e6e73`。
- 交互：悬停高亮与脉冲保持；点击分支时仅链路高亮，其余降为 12% 透明度（不用模糊/扫描线）。
- Dock：悬浮暖白胶囊底，主按钮暖橙渐变、白字。

## 4. 控件规范

- 主按钮 `.btn.primary`：暖橙渐变 `linear-gradient(135deg,#f59e0b,#ea580c)`，白字，圆角 12px，阴影 `0 6px 18px rgba(234,88,12,.26)`。
- 次按钮 `.btn.ghost`：白底/透明底 + 暖灰描边，hover 描边转橙。
- 输入框：白底、`rgba(140,115,90,.2)` 描边；focus 用 3px 暖橙光晕 `box-shadow: 0 0 0 3px rgba(233,137,12,.14)`。
- 标签 chip：白底暖灰字；选中态暖橙描边 + `#fff1e0` 底。
- 表格：细暖灰分隔线，hover 行 `rgba(255,241,224,.45)`。

## 5. 层级与留白

- 导航固定在顶部与内容之间，使用半透明白毛玻璃；移动端不横向挤压。
- 面板间距 ≥ 12px；卡片内边距 14-18px；区块标题 16-17px，无多余发光。
- 同一层级的内容横向对齐，操作按钮统一放在行尾或卡片底部。

## 6. 禁止事项

- 禁止黑底/深蓝底、全局网格线、扫描线、霓虹光晕、CRT 效果。
- 禁止 `#fff` 文字直接放在浅色背景上；文字色只用 `--text`/`--muted` 层级。
- 禁止把图表/装饰色用作正文颜色。
